// This is your active journey file — edit it to make the video your own.
// By default it re-exports the Mediterranean example so the project runs
// out-of-the-box. Replace the import with your own stops + timings.
//
// Schema reference: see ./example-mediterranean.js for the full type and
// inline docs on each field.

export { journey, timingFor, cameraFor } from './example-mediterranean.js';
