// Re-export the active journey from `journeys/your-trip.js`. Edit that file
// (or change this import) to switch trips. The schema lives in
// `journeys/README.md` and a fully-annotated reference in
// `journeys/example-mediterranean.js`.

export { journey, timingFor, cameraFor } from '../journeys/your-trip.js';
